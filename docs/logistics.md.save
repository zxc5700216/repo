VV请帮我开发一个“亚马逊物流处理系统”桌面网页应用，目标用户是中文运营人员，要求界面简单直观，支持上传 Excel 和 PDF，自动处理亚马逊发货相关数据，并导出结果文件。

一、项目目标
这是一个面向内部使用的物流处理工具，围绕 4 类 Excel 文件和 1 类 PDF 文件工作：

1. 装箱表 A
2. 发货模板 B
3. 包装箱信息表 C
4. 物流发票模板 D
5. 亚马逊导出的箱唛 PDF

系统需要实现：
1. 上传并解析 A/B/C/D/PDF
2. 自动抽取字段
3. 自动填写 B、C、D
4. 解析 PDF 每页信息
5. 支持按规则重命名 PDF
6. 展示每页 PDF 关键数据
7. 导出处理后的 Excel、PDF、汇总表、物流模板

二、技术要求
1. 前端使用现代 Web 技术开发，推荐 React + TypeScript。
2. 后端可用 Node.js，优先本地文件处理方案。
3. Excel 处理必须稳定，保留原始模板格式、公式、下拉框、样式。
4. PDF 需要支持逐页解析和逐页展示。
5. 所有处理逻辑尽量前后端分层，代码结构清晰。
6. UI 为中文。
7. 必须考虑后续扩展：以后可能新增“赛狐发货表”适配逻辑。
8. 上传后的数据要保存在当前会话状态中，支持用户重复调整和重新导出。
9. 所有导出文件名要有清晰规则。
10. 页面风格要求专业、简洁、偏业务系统风格，不要花哨。

三、真实文件结构说明
本次开发必须按以下真实文件结构适配，不要凭空假设。

-----------------------------------
A 表：装箱表
文件样本：
A-2026Trinka亚马逊表6-30us发.xlsx
-----------------------------------

1. A 表需要读取“最后一个 sheet”
真实样本 sheet：
- 2026.6.16（加）
- 6.30us发
要求：永远读取最后一个 sheet，例如本样本读取“6.30us发”

2. A 表关键字段所在区域
第 3 行是表头主行，关键字段包括：
- 产品图片-不嵌入
- 品名
- ASIN
- FNSKU
- Sku
- 包装尺寸（cm）
- 包装重量（kg）
- 海关编码 HS Code
- 采购成本（包含说明书，包装，运费，税点等）
- 发货总数

3. A 表中箱号分布规则
- 第 24 列为“发货总数”
- 第 25 列开始为箱号列，表头为：1、2、3、4、5……
- 每个 SKU 所在行，在箱号列下会有每箱装箱数量
- 某些 SKU 的前面箱号为空，后面箱号才有值，所以不能假设一定从第1箱开始连续填写
- 需要按列号识别箱号，而不是只按非空顺序编号

4. A 表样本中的关键信息示例
例如最后一个 sheet 中：
- SKU 在第 5 列
- 发货总数在第 24 列
- 箱号从第 25 列开始
- 某些 SKU 的 1-5 箱有值
- 某些 SKU 的 10-15 箱有值
- 某些 SKU 的 20-39 箱有值
所以必须做“动态箱号映射”

5. A 表需要抽取并保存的字段
对每一个 SKU，至少保存：
- 图片
- 中文品名
- ASIN
- FNSKU
- SKU
- 包装尺寸
- 包装重量kg
- 海关编码
- 采购成本
- 发货总数
- 各箱号对应数量明细
- 所在原始行号

6. A 表中还要读取整批箱信息
样本中存在：
- Weight of box (kg)
- Box length (cm)
- Box width (cm)
- Box height (cm)

这些信息在 A/C 的表格底部区域按箱号横向排列。
后续生成 C 表、汇总表、物流模板时都要用到。

-----------------------------------
B 表：发货模板
文件样本：
B-ManifestFileUpload_Template_IncludeCasePack_IncludeExpirationDate_IncludeMLC_MPL.xlsx
-----------------------------------

1. 需要先识别 B 表类型
识别规则：
如果存在 sheet 名为：
Create workflow – template
则认定为“亚马逊官方发货表”

本次先优先支持亚马逊官方发货表。
请预留赛狐发货表识别逻辑接口，但赛狐可先只做占位提示“暂未实现”。

2. 亚马逊官方发货表结构
真实 sheet：
- Instructions
- Data definitions
- Create workflow – template
- Create workflow – example

3. 需要写入的 sheet
Create workflow – template

4. 真实表头位置
第 6 行表头：
- Merchant SKU
- Quantity
- Expiration date (MM/DD/YYYY)
- Manufacturing lot code
- Units per box
- Number of boxes
- Box length (in)
- Box width (in)
- Box height (in)
- Box weight (lb)

5. 填充逻辑
从 A 表读取所有“发货总数非 0”的 SKU 行，逐行写入 B 表：
- A.SKU -> B.Merchant SKU
- A.发货总数 -> B.Quantity

写入起始行：
从 Create workflow – template 的第 7 行开始追加

6. 当前版本只要求写入：
- Merchant SKU
- Quantity

其余字段保持空白，不破坏模板原格式。

7. 导出
导出填写后的 B 表，保留原模板格式。
建议文件名：
B_已填写_时间戳.xlsx

-----------------------------------
C 表：包装箱包装信息表
文件样本：
C-1782993768800-20260702200248800.xlsx
-----------------------------------

1. 需要读取的 sheet
Pack List

2. C 表结构
真实样本中：
- 第 2 行是表头
- 第 8 列开始是第1箱、第2箱……
- 一直到第39箱
- 第 15 行是 Weight of box (kg)
- 第 16 行是 Box length (cm)
- 第 17 行是 Box width (cm)
- 第 18 行是 Box height (cm)

3. C 表填充逻辑
将 A 表与 C 表按 SKU 匹配：
- A.SKU == C.Pack List 中 SKU

如果匹配成功：
将 A 表对应箱号列的数量，填入 C 表对应箱号列下的单元格。

举例：
如果 A 某个 SKU 在箱号 1、2、3、4、5 下分别是 19、19、19、19、19
则将这些值写到 C 表该 SKU 行的：
- 第1箱
- 第2箱
- 第3箱
- 第4箱
- 第5箱

4. 箱号底部信息填充规则
C 表底部按箱号填写：
- 包装箱重量（磅）：来自 A 表或 C 表中箱重量 kg 换算成 lb
  换算公式：kg / 0.454
  建议保留 2 位小数
- 包装箱宽度（英寸）：默认 18
- 包装箱长度（英寸）：默认 17
- 包装箱高度（英寸）：默认 16

注意：
这里用户原话是“包装箱重量（磅）：来自装箱表A的重量KG右边的值/0.454”
所以请按箱号逐列处理。

5. 导出
导出填写后的 C 表，保留模板格式。
建议文件名：
C_已填写_时间戳.xlsx

-----------------------------------
PDF：亚马逊箱唛
文件样本：
货件3-LGB8-FBA19H4WYDTY-7箱-美森正班卡派.pdf
-----------------------------------

1. PDF 结构
一个 PDF 多页，每页代表一个箱子标签。
样本共 7 页。

2. 每页需要解析的字段
从每页文本中尽量提取：
- 货件号，例如：货件3
- 仓库名称，例如：LGB8
- FBA 编号，例如：FBA19H4WYDTY
- 总箱数，例如：7箱
- 箱位，例如：P1 - B4
- FBA 箱子编码，例如：FBA19H4WYDTYU000001
- SKU 类型：Single SKU / Mixed SKUs
- 如果页面中能提取到 SKU，则提取 SKU
- 如果页面中能提取到 Qty，则提取数量

3. PDF 的“公共信息”来源
样本中标题里已经包含：
货件3-LGB8-FBA19H4WYDTY-7箱-美森正班卡派

所以整份 PDF 的公共命名信息应优先从文件名或每页公共标题中识别：
- 货件号
- 仓库名
- FBA 编号
- 总箱数
- 渠道名

4. 每页页面展示内容
页面上要展示一个 PDF 解析结果列表，每页一条，至少包括：
- 页码
- 货件号
- 仓库名称
- FBA编号
- 总箱数
- 箱位
- FBA箱子编码
- SKU类型
- SKU（若有）
- Qty（若有）

5. PDF 重命名规则
对整份 PDF 生成规范主文件名：
货件3-LGB8-FBA19H4WYDTY-7箱.pdf

注意：
用户原文里有渠道名“美森正班卡派”，但又要求重命名为：
货件3-LGB8-FBA19H4WYDTY-7箱
所以主命名规则中不带渠道名。

6. 下载与操作
在 PDF 展示区域，每个 PDF 项右侧要有：
- 下载 PDF 按钮
- 一键生成物流模板 按钮

如果后续支持每页拆分下载，也请预留接口，但当前版本可先按整份 PDF 操作。

-----------------------------------
D 表：物流发票模板
文件样本：
D货件3-LGB8-FBA19H4WYDTY-7箱-美森正班卡派.xlsx
-----------------------------------

1. sheet 结构
- Sheet1
- FBA仓库地址

2. Sheet1 顶部订单信息区字段
真实样本中：
- 客户参考号1
- FBA仓码
- 收货渠道
- 目的地
- 总件数
- 报关类型
- 是否投保
- 是否带电
等

3. 物流模板顶部填充规则
- 客户参考号1：填入 FBA 编号，例如 FBA19H4WYDTY
- FBA仓码：填入仓库名称，例如 LGB8
- 总件数：填入箱数，只填数值，例如 7
- 报关类型：固定填“一般报关”
- 是否投保：固定填“是”
- 是否带电：固定填“否”
- 收货渠道：优先从 PDF 文件名解析，如“美森正班卡派”
- 目的地：固定“美国”

4. D 表明细区表头
从第 15 行开始，字段有：
- 箱序号 *
- FBAID*
- POAnumber*
- 单件实重*
- 长
- 宽
- 高
- 海关编码*
- 物品中文名 *
- 物品英文名 *
- 产品申报数量/每箱*
- 申报单价*
- 品牌
- 型号
- 材质(英文)*
- 用途(英文)
- 产品图片
等

5. D 表逐箱生成逻辑
系统需要支持：选择 D 模板后，一键为当前 PDF 生成对应物流模板。

生成方式：
按 PDF 页逐箱生成 D 表中的明细行。

每个箱子的明细字段规则：
- 箱序号：按顺序 1,2,3,4...
- FBAID*：填入该页的 FBA 箱子编码，例如 FBA19H4WYDTYU000001
- POAnumber*：不填
- 单件实重*：填入该箱对应重量，优先用 A/C 表该箱的重量 kg
- 长：默认 50
- 宽：默认 40
- 高：默认 40
- 海关编码*：来自 A 表
- 物品中文名*：来自 A 表
- 物品英文名*：来自 A 表，如果 A 表没有英文名，则需要预留手动编辑或后续维护；当前版本可先优先使用“系统字段/映射表/手工补录”
- 产品申报数量/每箱*：来自该箱对应的装箱数量
- 申报单价*：采购成本 / 8
- 品牌：填“无”
- 型号：填“无”
- 材质(英文)*：默认“尼龙/Nylon”
- 用途(英文)：默认“户外/Outdoor”
- 产品图片：拉取 A 表图片

6. 单件实重字段说明
这里要求填“对应的重量”，当前样本中 D 表现有数据填写的是每箱重量值。
所以当前版本按“箱重量”填入单件实重列，后续可再调整字段含义。

7. 多 SKU 箱处理逻辑
PDF 中有 Mixed SKUs 页面。
对于 Mixed SKUs：
- 需要根据 A/C 表中该箱号下有哪些 SKU 数量 > 0 来确定该箱包含哪些 SKU
- D 模板一箱是否写一行还是多行，要按业务要求设计为“一个箱子可展开成多条明细行”
- 即：同一箱如果含多个 SKU，则 D 表中该箱序号可重复多行，每个 SKU 一行
- 同一箱的 FBAID、长宽高、重量相同
- 每条明细的海关编码、中文名、英文名、申报数量、申报单价按该 SKU 分别填写

8. Single SKU 箱处理逻辑
若某页是 Single SKU，且能解析出 SKU 或 Qty，则优先用 PDF 页面数据校验 A/C 结果。
最终以 A/C 装箱数据为主，PDF 为辅助校验。

9. 英文名策略
A 表当前样本没有现成英文名字段，所以需要设计：
- 第一优先：如果系统已有 SKU 对应英文名缓存，则使用缓存
- 第二优先：尝试从既有模板 D 样本中同 SKU 历史记录反查
- 第三优先：允许用户在页面手动补录后生成
- 如果没有英文名，生成前给出提示，允许继续但标记缺失

10. 图片策略
A 表“产品图片-不嵌入”列可能是文字、链接或空值。
系统需要兼容：
- 图片链接
- 本地无图片
- 纯文本描述
若无法真正嵌入图片，则在 D 表中保留文本链接或空白，并给出提示。

四、装箱数据汇总表导出
系统要支持按现有数据生成一个“装箱数据汇总表”，格式参考用户提供的补货表风格。

需要输出内容至少包括：
- 重量/kg
- 数量
- 仓库名称
- 货件号
- SKU
- 最终发货
- 各箱号 1,2,3,4...n
- 物流渠道
- 物流单价
- 物流费用
- 入库配置费（美金）
- 总费用

规则说明：
1. 重量/kg：来自箱号对应重量
2. 数量：来自每箱每 SKU 数量
3. 仓库名称：来自 PDF 或模板
4. 货件号：来自 PDF 文件名或解析结果
5. SKU：来自 A/C
6. 最终发货：来自 A 表发货总数
7. 各箱号列：来自 A/C 装箱明细
8. 物流渠道：默认允许手工填写或从 PDF 文件名解析初值
9. 物流单价：允许手工填写
10. 物流费用：公式计算，建议 = 重量 * 物流单价
11. 入库配置费：允许手工填写
12. 总费用：公式汇总

注意：
这张汇总表需要可下载，格式整齐，适合业务人员直接使用。

五、系统 UI 设计要求
整体风格：
- 中文界面
- 类似企业内部操作台
- 顶部标题清晰
- 中间为主工作区
- 操作逻辑要线性、明确
- 不要做成复杂后台菜单系统
- 单页面即可完成主要操作

页面建议排版如下：

==================================================
顶部
==================================================
标题：亚马逊物流处理系统
副标题：上传装箱表、发货模板、包装箱表、PDF 箱唛、物流发票模板，自动生成发货文件与物流模板

右上角：
- 新建任务
- 清空当前数据
- 导出全部

==================================================
第一行：文件上传区
==================================================
做成 5 张卡片横向或两行布局，每张卡片有状态显示

卡片1：装箱表 A
- 上传按钮
- 已上传文件名
- 已识别 sheet 名
- 已提取 SKU 数量
- 查看明细按钮

卡片2：发货模板 B
- 上传按钮
- 模板类型识别结果
- 生成 B 表按钮
- 下载已填 B 表按钮

卡片3：包装箱表 C
- 上传按钮
- 已识别箱数
- 生成 C 表按钮
- 下载已填 C 表按钮

卡片4：箱唛 PDF
- 上传按钮
- 页数
- 已识别货件号/仓库/FBA编号
- 下载重命名 PDF 按钮

卡片5：物流模板 D
- 上传按钮
- 模板文件名
- 生成物流模板按钮
- 下载物流模板按钮

==================================================
第二行：处理结果总览
==================================================
左侧为“任务信息面板”
展示：
- 货件号
- 仓库名称
- FBA编号
- 总箱数
- SKU总数
- 发货总数
- 模板识别状态
- 缺失字段提醒

右侧为“操作按钮区”
按钮：
- 1. 解析全部文件
- 2. 生成 B 发货表
- 3. 生成 C 包装箱表
- 4. 解析 PDF
- 5. 生成汇总表
- 6. 生成物流模板
- 7. 一键全部导出

==================================================
第三行：主工作区
==================================================
左侧 55%：数据表格区
使用标签页切换：
- A表数据
- B表预览
- C表预览
- 汇总表预览
- 物流模板预览
- 异常日志

A表数据展示内容：
- SKU
- 品名
- 海关编码
- 采购成本
- 发货总数
- 箱号分布概览
- 图片状态

PDF解析列表展示：
- 页码
- 箱序号
- 箱位
- FBA箱子编码
- SKU类型
- SKU
- Qty
- 是否匹配A/C
- 操作

右侧 45%：PDF 预览区
上方：
- PDF 文件名
- 上一页 / 下一页
- 页码跳转
- 缩放
- 下载当前 PDF

下方：
- 当前页识别结果卡片
  - 货件号
  - 仓库名称
  - FBA编号
  - 箱数
  - 箱位
  - FBA箱子编码
  - SKU类型
  - SKU
  - Qty

再下方：
- 物流模板操作区
  - 模板选择下拉框
  - 生成当前页物流模板
  - 生成整票物流模板
  - 下载当前页模板
  - 下载整票模板

==================================================
底部
==================================================
日志区：
- 文件上传成功
- 识别到最后一个sheet
- 匹配到多少个SKU
- 多少个字段缺失
- 某些SKU未匹配
- 某些英文名缺失
- 某些图片缺失
- 某页PDF未识别到Qty
等

六、核心处理规则
1. 所有匹配以 SKU 为主键。
2. 箱号映射必须按真实列头匹配，不要假设从第1箱连续。
3. A 表最后一个 sheet 为准。
4. 发货总数为 0 的 SKU 不写入 B 表。
5. 生成 C/D 时要按箱号维度处理。
6. Mixed SKUs 箱必须支持拆多行。
7. 所有生成结果保留原模板格式。
8. 缺失字段不能直接报错退出，要给出“缺失提示 + 可继续处理”的机制。
9. 用户需要能预览处理结果后再下载。
10. 重要字段生成前做校验。

七、字段校验与异常处理
请实现完整异常处理：

1. A 表异常
- 没有 sheet
- 最后一个 sheet 为空
- 缺少 SKU / 发货总数 / 海关编码 / 采购成本
- 箱号列不存在
- 箱号数据与发货总数不一致

2. B 表异常
- 无 Create workflow – template
- 表头行结构变化
- 无法写入模板

3. C 表异常
- 无 Pack List
- SKU 未匹配到
- 箱号数量与 A 表不一致

4. D 表异常
- 缺少 Sheet1
- 缺少 FBA仓库地址
- 英文名缺失
- 图片缺失
- PDF 页数与箱数不一致

5. PDF 异常
- 无法解析页数
- 无法提取 FBA 箱子编码
- 无法提取箱位
- 文件名结构不符合规范

6. 所有异常必须在页面展示：
- 错误
- 警告
- 建议处理方式

八、推荐交互细节
1. 上传文件后自动识别类型与结构。
2. 识别成功后显示绿色状态。
3. 缺少关键字段显示黄色警告。
4. 处理失败显示红色错误。
5. 导出前弹出确认。
6. 对“英文名缺失”“图片缺失”提供可手动补录入口。
7. 用户修改后可重新生成。
8. 支持下载单个结果，也支持一键全部导出。

九、实现建议
请按模块开发：
1. 文件上传模块
2. Excel 解析模块
3. A 表数据标准化模块
4. B 表填充模块
5. C 表填充模块
6. PDF 解析模块
7. PDF 展示模块
8. D 表物流模板生成模块
9. 汇总表生成模块
10. 日志与异常模块

十、开发输出要求
请直接输出可运行项目代码，不要只给方案。
要求：
1. 先搭建完整页面
2. 再实现文件上传与解析
3. 再实现 A->B、A->C、PDF 解析、D 生成
4. 最后完善异常处理和 UI

十一、特别注意
1. 不要简化成纯演示版。
2. 不要只写假数据。
3. 要按真实上传文件结构适配。
4. Excel 模板必须尽量保留原格式。
5. PDF 解析结果要真实展示。
6. 页面必须是中文业务 UI，不要默认英文后台模板风格。
7. 如果某些字段无法自动获取，要提供“手动补录”能力，而不是直接失败。
8. 代码要有清晰注释，便于后续维护。
9. 需要预留“赛狐发货表”扩展点。
10. 请优先保证稳定性和准确性，其次再做视觉优化。

十二、当前样本已确认的真实结构摘要
A 表：
- 读取最后一个 sheet：6.30us发
- 第3行为关键表头
- SKU 在第5列
- 发货总数在第24列
- 箱号从第25列开始

B 表：
- 存在 Create workflow – template
- 第6行为表头
- 第7行开始写数据

C 表：
- Pack List sheet
- 第2行为表头
- 第8列开始是箱号列
- 第15-18行为箱重量和尺寸

D 表：
- Sheet1 为主模板
- FBA仓库地址 为仓库地址映射表
- 第15行为明细表头
- 第16行开始为明细行

PDF：
- 7页
- 每页可提取 FBA箱子编码
- 可提取箱位，如 P1 - B4
- 部分页是 Single SKU，部分页是 Mixed SKUs

请基于以上要求直接开始开发。
